import { useState } from 'react';
import { createCheckIn } from '../services/api';
import { fetchEmployees } from '../services/api';

export const CheckInForm = () => {
//    форма для более комплексного создания чекинов
};